*.swp

# AWS configuration - user-specific, generated by setup script
# Contains absolute paths that vary per user/machine
.aws/config
.aws/credentials
.aws/*.bak
.aws/*.tmp

# Claude Code - all files managed with alternate system
.claude/

# Backup files and directories created by setup scripts
*backup*
*.backup
*.bak
.dotfiles-backup-*
/backup/
/backups/
dotfiles-*.tar.gz.gpg
.vim/plugged/
.vim/.netrwhist

# SSH keys and certificates
id_*
*.pem
*.key
*.p12
*.pfx

# Environment files
.env
.env.*

# Password store
.password-store/

# Gemini / GCP credentials
.gemini/
application_default_credentials.json
